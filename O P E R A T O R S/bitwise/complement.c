#include <stdio.h>
void main()
{
    // one's complement of a number suppose given a=-(a+1)
    int a;
    a = -7;
    printf("%d\n", ~a);
    int s;
    s = 1 & printf("x");    
    printf("\n %d", s);
}