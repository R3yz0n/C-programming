#include <stdio.h>
void main()
{
    // int size, i = 0;

    // printf("enter the size of string  ");
    // scanf("%d", &size);
    // char name[size];
    // // fflush(stdin);
    // printf("enter the string  ");

    // gets(name);

    // while (name[i] != '\0')
    // {
    //     i++;
    // }
    // printf("the length of string is %d", i);

    
}